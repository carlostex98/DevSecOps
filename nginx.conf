events {}

http {
    upstream python_apps {
        server backend_1:8100;
        server backend_2:8100;
        server backend_3:8100;
        server backend_4:8100;
    }

    server {
        listen 90;

        location / {
            proxy_pass http://python_apps;
        }
    }
}
